spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cotato_hw1?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USERNAME:cotato_user}
    password: ${DB_PASSWORD:cotato1234}
    driver-class-name: com.mysql.cj.jdbc.Driver
    profiles:
      active: local

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect