spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cotato_hw1?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: cotato_user
    password: cotato1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database-platform: org.hibernate.dialect.MySQLDialect